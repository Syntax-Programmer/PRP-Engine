A window in the api is that rectangular box that the user can interact with.
Rest every thing about platform implementation is hidden away from the user.

The Init/Exit command initialize the envirnoment and set up the state to start
window creation.

Rest window create/delete creates that rectangular box thing that the users
consider as the window.
