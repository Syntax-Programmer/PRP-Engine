#include "Assert.h"
#include "Log.h"

PRP_FN_API DT_void PRP_FN_CALL DIAG_PanicLog(const DT_char *expr,
                                             const DT_char *file, DT_size line,
                                             const DT_char *func) {
                                                 DIAG_Log(DIAG_LOG_LEVEL_FATAL, DIAG_LogCode , const DT_char *file, DT_size line, const DT_char *func, const DT_char *msg, ...)
                                             }
